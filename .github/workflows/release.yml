name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Get version
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create .deb package
        run: |
          sudo apt-get update
          sudo apt-get install -y dpkg-dev
          ./packaging/deb/build.sh ${{ steps.version.outputs }} amd64

      - name: Create .rpm package
        run: |
          sudo apt-get update
          sudo apt-get install -y rpmbuild
          ./packaging/rpm/build.sh ${{ steps.version.outputs }} x86_64

      - name: Rename packages
        run: |
          mv tux_${{ steps.version.outputs }}_amd64.deb tux_${{ steps.version.outputs }}_amd64.deb
          mv tux-${{ steps.version.outputs }}-1.x86_64.rpm tux-${{ steps.version.outputs }}.x86_64.rpm

      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            tux_${{ steps.version.outputs }}_amd64.deb
            tux-${{ steps.version.outputs }}.x86_64.rpm
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
